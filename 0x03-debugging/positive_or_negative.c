#include "main.h"

/**
 *positive_or_negative - for positive or negative
 *
 * @i: parameter
 *
 * Return: void
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);
}
