#include <stdio.h>

/**
 * main - starting point of execution and is returning an integer
 *
 * Return: to obey the void
 */

int main(void)
{	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
